<UserControl x:Class="Kabutar_WPF.Views.UserCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0"
                Background="{DynamicResource PrimaryBrush}"
                Padding="30,40,30,30">
            <StackPanel HorizontalAlignment="Center">

                <Grid Width="100" Height="100" Margin="0,0,0,15">

                    <Border Width="100" Height="100"
                            CornerRadius="50"
                            Background="White">
                        <TextBlock x:Name="UserInitials"
                                   Text=""
                                   Foreground="{DynamicResource PrimaryBrush}"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>


                    <Border x:Name="UserImageBorder"
                            Width="100" Height="100"
                            CornerRadius="50"
                            Visibility="Collapsed"
                            Cursor="Hand"
                            MouseLeftButtonUp="UserImageBorder_MouseLeftButtonUp">
                        <Border.Background>
                            <ImageBrush x:Name="UserImage"
                                        Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                </Grid>


                <TextBlock x:Name="UserFullName"
                           Text=""
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>


                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,8,0,0">
                    <Ellipse x:Name="OnlineIndicator"
                             Width="10" Height="10"
                             Fill="#4CAF50"
                             Margin="0,0,6,0"
                             Visibility="Collapsed"/>
                    <TextBlock x:Name="OnlineStatus"
                               Text="Offline"
                               FontSize="13"
                               Foreground="#E0FFFFFF"/>
                </StackPanel>
            </StackPanel>
        </Border>


        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="25,25">

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Username"
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="UserUsername"
                               Text=""
                               FontSize="16"
                               Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>


                <StackPanel x:Name="AboutSection" Margin="0,0,0,20">
                    <TextBlock Text="Haqida"
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="UserAbout"
                               Text=""
                               FontSize="14"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>


                <Button Content="Xabar yuborish"
                        Click="SendMessage_Click"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="White"
                        Padding="20,12"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </ScrollViewer>


        <Button Grid.Row="0"
                Content="&#x2715;"
                Click="Close_Click"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="15"
                Width="30" Height="30"
                Background="#33FFFFFF"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="15">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#55FFFFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
