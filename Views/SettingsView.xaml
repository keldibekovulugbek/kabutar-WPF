<UserControl x:Class="Kabutar_WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0"
                Background="{DynamicResource PrimaryBrush}"
                Padding="20,15">
            <Grid>
                <TextBlock Text="Sozlamalar"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
                <Button Content="&#x2715;"
                        Click="Close_Click"
                        HorizontalAlignment="Right"
                        Width="30" Height="30"
                        Background="#33FFFFFF"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#55FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>


        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="25,20">


                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20,15"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Bildirishnomalar"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"
                                   Margin="0,0,0,14"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Xabarlar bildirishnomасini ko'rsatish"
                                       FontSize="14"
                                       Foreground="{DynamicResource TextBrush}"
                                       VerticalAlignment="Center"/>
                            <ToggleButton Grid.Column="1"
                                          x:Name="NotificationsToggle"
                                          IsChecked="True"
                                          Width="50" Height="26"
                                          Cursor="Hand">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="ToggleBorder"
                                                            Background="#CCCCCC"
                                                            CornerRadius="13"
                                                            Width="50" Height="26">
                                                        <Border x:Name="Thumb"
                                                                Width="22" Height="22"
                                                                CornerRadius="11"
                                                                Background="White"
                                                                HorizontalAlignment="Left"
                                                                Margin="2,0,0,0">
                                                            <Border.Effect>
                                                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="4" ShadowDepth="1"/>
                                                            </Border.Effect>
                                                        </Border>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </Grid>
                    </StackPanel>
                </Border>


                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20,15"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Matn o'lchami"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"
                                   Margin="0,0,0,14"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                            <RadioButton x:Name="SmallFontRadio"
                                         Content="Kichik (12px)"
                                         GroupName="FontSize"
                                         FontSize="13"
                                         Foreground="{DynamicResource TextBrush}"
                                         Margin="0,0,20,0"
                                         Cursor="Hand"
                                         Checked="FontSize_Checked"
                                         Tag="12"/>
                            <RadioButton x:Name="MediumFontRadio"
                                         Content="O'rta (14px)"
                                         GroupName="FontSize"
                                         IsChecked="True"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextBrush}"
                                         Margin="0,0,20,0"
                                         Cursor="Hand"
                                         Checked="FontSize_Checked"
                                         Tag="14"/>
                            <RadioButton x:Name="LargeFontRadio"
                                         Content="Katta (16px)"
                                         GroupName="FontSize"
                                         FontSize="15"
                                         Foreground="{DynamicResource TextBrush}"
                                         Cursor="Hand"
                                         Checked="FontSize_Checked"
                                         Tag="16"/>
                        </StackPanel>
                    </StackPanel>
                </Border>


                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20,15"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Ilova haqida"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"
                                   Margin="0,0,0,14"/>

                        <TextBlock Text="Versiya: 1.0.0"
                                   FontSize="14"
                                   Foreground="{DynamicResource TextBrush}"
                                   Margin="0,0,0,8"/>

                        <TextBlock Text="Kabutar — xavfsiz va tez messenger"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
