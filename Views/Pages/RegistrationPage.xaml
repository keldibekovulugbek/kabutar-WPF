<Page x:Class="Kabutar_WPF.Views.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Kabutar_WPF.Views.Pages"
      mc:Ignorable="d" 
      Title="RegistrationPage">

    <Border Padding="20">
        <!-- Apply padding here -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="2">
                <Button Content="Register"  Width="200" Margin="0,10,0,0" Background="{StaticResource AccentColor}" Foreground="White" Height="30"/>
                <Button Content="Already have an account?" Background="Transparent" Foreground="{StaticResource AccentColor}" BorderBrush="Transparent" Width="200" Margin="0,10,0,0" Height="30"/>
            </StackPanel>
        </Grid>
    </Border>
</Page>
