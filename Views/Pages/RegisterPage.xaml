<Page x:Class="Kabutar_WPF.Views.Pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:Kabutar_WPF.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:RegisterViewModel}"
      mc:Ignorable="d"
      Title="RegisterPage">

    <Grid DataContext="{Binding}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Create an account" FontSize="24" HorizontalAlignment="Center"/>

            <TextBlock Text="First Name" HorizontalAlignment="Left"/>
            <TextBox Width="200" Text="{Binding Firstname, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Last Name" HorizontalAlignment="Left"/>
            <TextBox Width="200" Text="{Binding Lastname, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Email" HorizontalAlignment="Left"/>
            <TextBox Width="200" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Username" HorizontalAlignment="Left"/>
            <TextBox Width="200" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Password" HorizontalAlignment="Left"/>
            <TextBox Width="200" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding PasswordError}" Foreground="Red" FontSize="12"
                       Visibility="{Binding PasswordError, Converter={StaticResource StringToVisibilityConverter}}"/>

            <Button Content="Register" Width="200"
                    Command="{Binding RegisterCommand}"
                    IsEnabled="{Binding RegisterCommand.CanExecute, Mode=OneWay}"/>
            <Button Content="I've already an account" Background="Transparent"
                    Foreground="{StaticResource AccentColor}" BorderBrush="Transparent"
                    Width="200" Margin="0,10,0,0" Height="30"
                    Click="BackToLoginButton_Click"/>

        </StackPanel>
    </Grid>
</Page>
